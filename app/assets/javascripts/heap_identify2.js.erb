

// $(document).ready(function(){
	//  SAMPLE CODE -- can use ERB file to identify non-state based values from data layer in JS files.
	// $('#currentUserH1').css('background-color','gray');
	// var user_count = <%= User.count %>
	// console.log("user count: "+user_count);	
// });