<h1>Review summary</h1>

<h3>Total Reviews: <%= @reviews.count %></h3>
<h3>Average rating: <%= Review.average_rating %></h3>
<h3>Reviews with both rating & nps: <%= Review.nps_eligible.count %> </h3>
<h3>NPS Score: <%= Review.average_nps %></h3>

<table>
  <thead>
    <tr>
      <th>Instructor</th>
      <th>Lesson</th>
      <th>Reviewer</th>
      <th>Rating</th>
      <th>NPS</th>
      <th>Review</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @reviews.each do |review| %>
      <tr>
        <td><%= review.instructor_id %></td>
        <td><%= review.lesson_id %></td>
        <td><%= review.reviewer_id %></td>
        <td><%= review.rating %></td>
        <td><%= review.nps %></td>
        <td><%= review.review %></td>
        <td><%= link_to 'Show', review %></td>
        <td><%= link_to 'Edit', edit_review_path(review) %></td>
        <td><%= link_to 'Destroy', review, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Review', new_review_path %>
